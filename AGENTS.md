# 项目级 Codex 规范

## 项目目标

本仓库用于从 0 复刻一个 C++ AI 语音面试系统。复刻目标是学习和重建能力，不是逐行复制旧项目。

目标产品包含：

- C++ 面试问题生成与管理。
- 候选人回答记录、评分、追问和报告。
- 可选 PDF 简历解析。
- 可选 LLM API、实时语音 WebSocket、PortAudio 音频、Qt 桌面 UI。

## 沟通方式

- 默认使用中文回复。
- 关键技术名词在需要时使用“中文解释 + English 原词”。
- 代码注释默认使用中文，方便不依赖 AI 的读者理解代码意图；必要时保留英文技术名词。
- 面向新手解释时优先讲目标、设计原因、数据流、验证方式。

## 学习优先级

- 先保证能运行，再补完整功能。
- 先写 mock，再接真实服务。
- 先命令行程序，再 Qt UI。
- 先单线程和确定性测试，再引入线程、音频、网络。
- 每次只做一个可验证小目标。
- 参考源项目当前 CMake 入口是 Qt GUI；如果为了学习先做 CLI，请明确这是学习脚手架，不要把旧 CLI 入口当作可直接复刻的当前入口。

## 推荐复刻顺序

1. CMake 项目骨架、`main.cpp`、基础测试。
2. `common`：配置、日志、状态枚举、工具函数。
3. `interview`：问题、回答、评分、报告的纯 C++ 逻辑。
4. `services`：LLM/PDF/audio/realtime 的接口和 mock。
5. 真实 LLM HTTP 客户端。
6. PDF 解析适配器。
7. 二进制协议编解码与测试。
8. 对话编排、状态机、事件回调。
9. PortAudio 音频适配器。
10. WebSocket 实时服务适配器。
11. Qt 主窗口、配置对话框、状态与进度显示。
12. README、CI、GitHub PR 流程、打包说明。

## C++ 代码规范

- 使用 C++17 或更高版本，除非当前阶段明确降低要求。
- 头文件统一使用 `#pragma once`。
- 头文件必须自包含；用到的标准库、Qt 类型必须在本文件直接 include，不依赖传递 include。
- 禁止 `using namespace std;`。
- 禁止在全局作用域、命名空间作用域、匿名命名空间中集中写 `using` 声明。
- 项目代码放在具名命名空间中，例如 `interview::common`。
- 单参数构造函数使用 `explicit`。
- 优先 Rule of Zero，只有自己管理资源时再考虑 Rule of Five。
- 输入参数优先 `const T&`，轻量类型按值传递。
- 输出优先返回值，不优先使用输出参数。
- 不返回局部对象的指针或引用。
- 优先 `std::unique_ptr` 表示唯一所有权，避免裸 owning pointer。
- 复杂外部依赖使用接口隔离，方便 mock 和测试。
- 每次新增或修改代码时，都要为新增逻辑、关键边界、测试意图补充中文注释；注释说明“为什么这样做”和“验证什么”，不要只重复代码字面意思。

## 中文注释要求

- 本仓库是学习型复刻项目，新增或修改 C++ 代码时默认要补中文注释；不能以“代码很简单”为理由完全省略注释。
- 注释是代码完成标准的一部分，不是事后可选项；如果新增代码缺少面向新手的解释，本次任务默认视为没有完成，必须先补注释再继续新功能。
- 头文件里的 public API 必须优先补注释，包括公开 `struct`、`class`、接口、虚函数、请求/响应字段和会被其他模块调用的函数。
- `.cpp` 中的非平凡逻辑必须补注释，包括 mock 规则、边界返回、评分阈值、状态转换、资源生命周期、错误处理、安全取舍和依赖隔离原因。
- 外部服务相关代码必须额外解释清楚：这一层为什么存在、谁拥有连接/资源、是否会阻塞、错误怎么向上层传播、为什么默认测试不能联网、真实密钥从哪里注入且不能提交。
- 协议、WebSocket、音频、Qt、线程、adapter、factory、mock/fake 测试这类新手高风险代码，必须在关键数据流前写中文注释，说明输入、输出、边界条件和后续替换点。
- 测试文件中每个新增测试用例前都要用中文说明“验证什么”和“为什么这个边界重要”；测试内部关键构造数据也应补一两句注释说明意图。
- 注释要服务学习和维护，优先解释设计原因、数据流、边界条件和未来替换点；避免只复述变量赋值、循环自增这类字面行为。
- 使用 `clang-format` 后要复查注释和 `#include` 顺序；如果格式化工具破坏项目约定，可以用局部 `// clang-format off/on` 保护 include 分组。
- 完成代码改动前必须自检本次新增/修改的 `.h`、`.cpp`、测试文件是否有足够中文注释；如果用户指出注释不足，先补注释，再继续新功能。
- 自检时按这个顺序看：public API 是否解释用途和调用时机；private helper 是否解释不直观的协议/状态转换；测试 fake 是否解释为什么不用真实服务；错误处理是否解释为什么抛异常、返回 false 或转成内部事件。

## 文件与命名

- 文件名使用小写下划线，例如 `interview_session.h`。
- 类型名使用 PascalCase。
- 函数名统一使用 PascalCase（大写字母开头的驼峰），例如 `LoadConfigFromFile`、`CreateLlmClient`。
- 普通变量、函数形参使用 snake_case。
- 类成员变量以 `_` 结尾。
- 常量优先使用 `kNameStyle`。
- 枚举值优先使用 `kValueStyle`。

## 依赖与密钥

- 不提交真实 API Key、App ID、Access Key、App Key、Token、私有 URL。
- 提交 `config.example.json`，本地使用被 `.gitignore` 忽略的 `config.local.json`。
- 不在日志里输出完整 LLM 请求、API key、简历全文、候选人完整回答或服务端鉴权 header。
- HTTPS/WSS 默认必须开启证书校验。只有在本地诊断时才允许临时关闭，并且不能提交。
- 单元测试不能依赖网络、麦克风、扬声器、真实 PDF 系统环境或外部 API。
- 真实 LLM、PDF、音频、WebSocket 检查放到手动集成测试。
- 新增第三方库前说明用途、替代方案、是否必须。
- 新增依赖时同步更新 `vcpkg.json`、`CMakeLists.txt` 和构建说明。

## 并发与 Qt

- 不新增捕获裸 `this` 的 detached thread。
- 每个线程必须有明确启动点、停止点和 join/退出策略。
- WebSocket 读、写、关闭必须有明确线程归属或串行化策略，不能多个线程同时读同一个 socket。
- Qt UI 只能在主线程更新；后台回调进入 UI 必须使用 queued connection 或等价机制。
- 对象析构前必须取消回调或保证回调不会访问已销毁对象。

## 构建与验证

优先使用这些命令，按当前仓库实际情况调整：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build -j
ctest --test-dir build --output-on-failure
```

如果存在 `build.py`：

```bash
python3 build.py --config Debug
```

提交前尽量运行：

```bash
clang-format -i <changed-cpp-files>
cmake --build build -j
ctest --test-dir build --output-on-failure
```

如果暂时不能运行某个命令，说明原因和剩余风险。

## 闭环与推进规则

- 每次开始新功能前，先判断当前 milestone 是否已经“形成闭环”；如果当前阶段只是“接口已建好、主流程未接通、测试只覆盖局部”，默认优先把当前 milestone 收口，不要急着跳到下一个大模块。
- 如果已经新增了接口、mock、配置、报告或测试中的任意一层，但数据流还没有从入口走通到出口，优先补成最小可验证闭环，再考虑真实服务、PDF、协议、音频、Qt 或并发能力。
- 默认优先补“当前最明显的架构断点”，不要在已有模块还悬空时横向扩展新子系统。
- 当用户要求“继续下一步”而工作区里还有未提交改动时，先汇总 `git status --short`、`git diff --stat`、测试状态、当前 milestone 是否闭环，再决定是继续收口还是进入下一个功能。
- 如果当前未提交改动来自 Codex 上一步实现，且它已经形成独立学习闭环，默认建议先完成一次本地提交后再继续下一个功能；除非用户明确要求暂不提交。

## Codex 使用规则

- 做项目功能时优先使用 `$rebuild-cpp-interview`。
- 讲解 C++ 概念、复盘代码、出练习时优先使用 `$cpp-learning-mentor`。
- 大改动前先让 Codex 给出短计划。
- 当用户要求“规划、方案、评估、下一步任务设计”时，只做只读分析和文字规划，不修改代码、不写文件、不提交；只有用户明确要求“实现、修改、提交、生成文件”时才进入编辑阶段。
- 进入实现阶段前，要明确本次会修改哪些文件、学习目标是什么、计划用什么命令验证；实现后要报告注释补充点、验证结果和未完成风险。
- Codex 生成或修改代码后，必须主动做一次“注释自检”，重点看新增 public API、mock 行为、测试用例和边界处理是否已有中文解释。
- 如果本次实现包含新的接口或抽象层，结束前必须说明“这个抽象是否已真正接入主流程”；如果没有接入，要明确把“接通主流程”列为下一步优先事项。
- 遇到构建依赖、协议、LLM 安全、并发、Qt 生命周期、GitHub PR 时，可以用 `.codex/subagents/` 中的子代理提示词开独立检查。
- 子代理只用于独立复核、测试计划、PR 检查等边界清晰的任务，不让它们同时改同一批文件。
- 每次完成一个目标功能后，根据当前上下文把 Codex 对话标题改成简短、具体、便于回看历史的标题；如果工具需要 thread id，就必须继续查到当前线程并完成改名，不能停在“发现了工具但没执行”。
- 标题优先使用“当前最小闭环功能名”，例如“配置驱动的 mock 面试闭环”“接入 MockLlmClient 到 InterviewManager”，不要继续保留“规划下一步实现”这类宽泛标题。
- 如果本次实现或规划已经得出明确下一步，最终回复中要顺手检查线程标题是否仍停留在过时阶段名；如果过时，优先修正。
- 每完成一个独立功能闭环后，都要在 `development_records/` 下补一份对应的中文 Markdown 记录，说明本次目标、核心数据流、关键 C++ 知识点、验证结果和下一步建议。

## GitHub 提交流程

- 分支命名：`codex/<short-topic>` 或 `learn/<short-topic>`。
- Codex 一旦进入实现阶段（修改代码、文档、配置或测试前），必须先检查当前分支；如果当前在 `main`，先基于 `main` 新建 `codex/<short-topic>` 或 `learn/<short-topic>` 分支，再开始实现，禁止直接在 `main` 上完成实现提交。
- 如果当前已经在非 `main` 分支，但该分支不属于本次任务，先切回 `main` 再新建本次任务分支，避免把无关改动混进同一个 MR。
- 每次提交只包含一个学习目标或一个功能闭环。
- 提交前查看 `git status --short`、`git diff --stat`、`git diff`。
- Codex 生成的 commit message 默认使用中文，保留 Conventional Commits 类型前缀：
  - `feat: 添加面试会话骨架`
  - `test: 覆盖配置校验`
  - `docs: 补充复刻阶段说明`
  - `refactor: 抽取 LLM 客户端接口`
- PR 描述必须包含：目标、主要改动、验证结果、未完成事项。

## 禁止事项

- 不要为了统一风格做大规模无收益重构。
- 不要把旧项目大段源码直接复制进来。
- 不要把真实密钥写进代码、测试、截图、日志或提交历史。
- 不要在没有测试或手动验证说明的情况下接入复杂外部服务。
- 不要让 UI 直接依赖具体网络或音频实现。
