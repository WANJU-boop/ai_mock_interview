#pragma once

namespace interview {
namespace services {

// Keeps the placeholder service target non-empty while the service layer
// is still under construction.
void linkServiceLayerPlaceholder();

}  // namespace services
}  // namespace interview
